---
container:
  name: "jellyfin"
  config:
    dir: "/config"
  cache:
    dir: "/cache"
image:
  name: "ghcr.io/linuxserver/jellyfin:latest"
config:
  dir: "/home/{{ username }}/jellyfin/config"
cache:
  dir: "/home/{{ username }}/jellyfin/cache"
